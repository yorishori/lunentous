services:
  lunentous:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    environment:
      - TZ=${TZ:-UTC}
      - PORT=8080
      - DB_PATH=/data/db.sqlite
      - PHOTOS_DIR=/data/photos
    volumes:
      - ./server/data:/data
    restart: unless-stopped
